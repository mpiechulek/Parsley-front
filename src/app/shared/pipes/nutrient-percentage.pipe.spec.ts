import { NutrientPercentagePipe } from './nutriens-percentage.pipe';

describe('NutrientPercentagePipe', () => {
  it('create an instance', () => {
    const pipe = new NutrientPercentagePipe();
    expect(pipe).toBeTruthy();
  });
});
